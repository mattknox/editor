this is intended to be the beginnings of a programmer's text editor implemented in HTML/javascript, in the same spirit as emacs.
The main goal is to be extensible in javascript/coffeescript/sibilant/whatever else compiles to js, and also styleable in HTML/CSS.


need to build:
  parser framework
  macros to transform source code into classed code, for colorization.
  data structures in js.
  strip down ymacs and make it single brawer and awesome
